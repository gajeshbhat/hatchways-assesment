Ping Hatchways Blog
---
tags:
  - Ping
parameters:
  - in: path
    name: None
    type: None
    required: false
responses:
  200:
    description: Returns true if the server is up and running.
    schema:
      id: ping_get
      properties:
        success:
          type: string
          description: True means server is up and running else false.
          default: 'True'